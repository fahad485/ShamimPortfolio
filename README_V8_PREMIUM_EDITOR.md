# V8 Premium Section Editor

- Every major portfolio section now has an Edit Section gate and Premium Design Studio.
- 10 layout presets per major section type.
- 30 browser-safe font presets.
- Section-level typography, colors, hover/link controls, paragraph image, CTA, background image/color, opacity, blur, brightness, contrast, saturation, overlay, radius and shadow.
- Global scientific DNA/chemical background remains continuous and fixed by default.
- Mobile layouts collapse safely to one column and preserve readable text.
- Design settings are stored inside `data/appearance.json` under `sectionStyles`.
